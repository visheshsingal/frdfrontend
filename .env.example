# Frontend env example (Vite)
VITE_API_URL=http://localhost:4000
VITE_AUTH0_DOMAIN=your-auth0-domain.auth0.com
VITE_AUTH0_CLIENT_ID=your_auth0_client_id
VITE_AUTH0_REDIRECT_URI=http://localhost:5173/auth0/callback

# Notes:
# 1) Create an Auth0 Application (Single Page Application) and enable Google connection.
# 2) Set Allowed Callback URLs to the redirect URI above.
# 3) Install dependencies if you plan to use official SDKs. This implementation uses a simple redirect flow.
